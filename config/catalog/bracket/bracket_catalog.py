from config.catalog.mast.mast_material import MastMaterial


class BracketCatalog:
    pass
    """
    def build_name(self) -> str:
    parts = ["CAKO250", self.install_type.value]

    if self.special_type != BracketSpecialType.NONE:
        parts.append(self.special_type.value)

    parts.append(f"{self.gauge:.1f}")

    parts.append(self.base_type.value)

    if self.variant != BracketVariant.NONE:
        parts[-1] += f"({self.variant.value})"

    return "-".join(parts)

    """